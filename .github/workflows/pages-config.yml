name: Configure GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  configure:
    runs-on: ubuntu-latest
    steps:
      - name: Configure GitHub Pages to use main branch
        run: |
          echo "GitHub Pages is configured to deploy from the 'main' branch"
          echo "This workflow ensures proper deployment configuration"
