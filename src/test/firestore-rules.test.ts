import { describe, it, expect } from "vitest";

describe("Firestore Rules", () => {
  it.skip("User autenticato può creare propria quote", () => {
    expect(true).toBe(true);
  });

  it.skip("User NON può leggere quote di altri", () => {
    expect(true).toBe(true);
  });

  it.skip("Unauthenticated user NON può creare quote", () => {
    expect(true).toBe(true);
  });

  it.skip("Quote deve avere struttura valida", () => {
    expect(true).toBe(true);
  });
});
